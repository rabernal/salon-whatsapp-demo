{
  "name": "salon-whatsapp-demo",
  "version": "1.0.0",
  "description": "Recordable demo: a WhatsApp-style AI booking assistant for a Spanish-speaking salon. Runs offline (mock) or live with Claude.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "db:show": "tsx scripts/db-show.ts",
    "test": "tsx scripts/test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "better-sqlite3": "^11.8.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
